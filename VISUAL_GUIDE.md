# 🎨 Keploy TypeScript Quickstart - Visual Guide

```
┌─────────────────────────────────────────────────────────────────┐
│                 KEPLOY TYPESCRIPT QUICKSTART                    │
│         Express + TypeScript + PostgreSQL + Prisma              │
└─────────────────────────────────────────────────────────────────┘
```

## 📊 Architecture Overview

```
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│              │      │              │      │              │
│    Client    │─────▶│  Express.js  │─────▶│  PostgreSQL  │
│  (curl/API)  │      │   Server     │      │   Database   │
│              │◀─────│  (Port 8000) │◀─────│  (Port 5432) │
└──────────────┘      └──────────────┘      └──────────────┘
                             │
                             │ Keploy intercepts
                             ▼
                      ┌──────────────┐
                      │              │
                      │    Keploy    │
                      │   Recording  │
                      │              │
                      └──────────────┘
```

## 🗂️ Project Structure

```
keploy-typescript-quickstart/
│
├── 📁 src/                          # Source code
│   ├── index.ts                    # Main Express server
│   ├── db.ts                       # Prisma client
│   └── routes/
│       └── users.ts                # User CRUD endpoints
│
├── 📁 prisma/                       # Database
│   └── schema.prisma               # Schema definition
│
├── 📁 scripts/                      # Helper scripts
│   ├── record.sh                   # Record locally
│   ├── test.sh                     # Test locally
│   ├── docker-record.sh            # Record with Docker
│   ├── docker-test.sh              # Test with Docker
│   ├── test-api.sh                 # API test suite
│   └── verify-setup.sh             # Environment check
│
├── 📁 docs/                         # Documentation
│   ├── README.md                   # Complete guide
│   ├── GETTING_STARTED.md          # Beginner guide
│   ├── QUICKSTART.md               # Quick reference
│   ├── OVERVIEW.md                 # Project overview
│   └── PROJECT_SUMMARY.md          # Summary
│
├── 🐳 Docker files
│   ├── Dockerfile                  # App container
│   └── docker-compose.yml          # Multi-container
│
├── ⚙️ Configuration
│   ├── package.json                # Dependencies
│   ├── tsconfig.json               # TypeScript config
│   ├── .env                        # Local env vars
│   ├── .env.docker                 # Docker env vars
│   └── .gitignore                  # Git ignore
│
└── 📝 Other
    └── requests.http               # Sample API calls
```

## 🔄 API Request Flow

```
1. Client makes request
   │
   ├─▶ POST /users
   │   └─▶ Create new user
   │
   ├─▶ GET /users
   │   └─▶ List all users
   │
   ├─▶ GET /users/:id
   │   └─▶ Get specific user
   │
   ├─▶ PUT /users/:id
   │   └─▶ Update user
   │
   └─▶ DELETE /users/:id
       └─▶ Delete user

2. Express routes request
   │
   └─▶ src/routes/users.ts
       │
       ├─▶ Validate input
       │
       ├─▶ Query database (Prisma)
       │
       └─▶ Return response

3. Keploy records everything
   │
   ├─▶ HTTP request
   ├─▶ Database query
   ├─▶ HTTP response
   └─▶ Database result
```

## 🎬 Keploy Workflow

### Record Mode

```
┌──────────────────────────────────────────────────────────┐
│ STEP 1: Start Recording                                  │
└──────────────────────────────────────────────────────────┘
$ npm run keploy:record

┌──────────────────────────────────────────────────────────┐
│ STEP 2: Keploy Starts Your App                          │
└──────────────────────────────────────────────────────────┘
🚀 Server running on http://localhost:8000

┌──────────────────────────────────────────────────────────┐
│ STEP 3: Make API Calls                                   │
└──────────────────────────────────────────────────────────┘
$ curl -X POST http://localhost:8000/users \
    -H "Content-Type: application/json" \
    -d '{"name":"Alice","email":"alice@example.com"}'

┌──────────────────────────────────────────────────────────┐
│ STEP 4: Keploy Records                                   │
└──────────────────────────────────────────────────────────┘
📹 Request recorded
📹 Database query recorded
📹 Response recorded
💾 Test case saved: test-1.yaml

┌──────────────────────────────────────────────────────────┐
│ STEP 5: Stop Recording                                   │
└──────────────────────────────────────────────────────────┘
Press Ctrl+C
✅ 5 test cases recorded
```

### Test Mode

```
┌──────────────────────────────────────────────────────────┐
│ STEP 1: Start Testing                                    │
└──────────────────────────────────────────────────────────┘
$ npm run keploy:test

┌──────────────────────────────────────────────────────────┐
│ STEP 2: Keploy Replays Tests                            │
└──────────────────────────────────────────────────────────┘
▶️  Replaying test-1: POST /users
    ├─ Sending request...
    ├─ Mocking database...
    ├─ Comparing response...
    └─ ✅ PASSED

▶️  Replaying test-2: GET /users
    ├─ Sending request...
    ├─ Mocking database...
    ├─ Comparing response...
    └─ ✅ PASSED

┌──────────────────────────────────────────────────────────┐
│ STEP 3: View Results                                     │
└──────────────────────────────────────────────────────────┘
📊 Test Results: 5/5 passed (100%)
```

## 🐳 Docker Setup

```
┌─────────────────────────────────────────────────────┐
│              Docker Compose Services                │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌─────────────┐    ┌─────────────┐               │
│  │   App       │───▶│ PostgreSQL  │               │
│  │ (Port 8000) │    │ (Port 5432) │               │
│  └─────────────┘    └─────────────┘               │
│         │                                           │
│         │ monitored by                             │
│         ▼                                           │
│  ┌─────────────┐                                   │
│  │   Keploy    │                                   │
│  │  (Profile)  │                                   │
│  └─────────────┘                                   │
│                                                     │
└─────────────────────────────────────────────────────┘

Commands:
  $ docker-compose up -d              # Start all
  $ docker-compose --profile record up # Record mode
  $ docker-compose --profile test up   # Test mode
  $ docker-compose logs -f             # View logs
  $ docker-compose down                # Stop all
```

## 📈 Development Flow

```
┌─────────────┐
│   Start     │
└─────┬───────┘
      │
      ▼
┌─────────────────┐
│ Install & Setup │  npm install
└─────┬───────────┘  npm run prisma:generate
      │
      ▼
┌─────────────────┐
│  Start Server   │  npm run dev
└─────┬───────────┘
      │
      ▼
┌─────────────────┐
│  Test Manually  │  curl commands or
└─────┬───────────┘  ./scripts/test-api.sh
      │
      ▼
┌─────────────────┐
│ Record Tests    │  npm run keploy:record
└─────┬───────────┘
      │
      ▼
┌─────────────────┐
│  Run Tests      │  npm run keploy:test
└─────┬───────────┘
      │
      ▼
┌─────────────────┐
│    Deploy       │  docker-compose up -d
└─────────────────┘
```

## 🔐 Database Schema

```
┌─────────────────────────────────────┐
│            users table              │
├─────────────────────────────────────┤
│ id         INT (PK, AUTO_INCREMENT) │
│ name       VARCHAR (NOT NULL)       │
│ email      VARCHAR (UNIQUE)         │
│ createdAt  TIMESTAMP                │
│ updatedAt  TIMESTAMP                │
└─────────────────────────────────────┘
```

## 📝 API Endpoints Map

```
┌──────────────────────────────────────────────────────┐
│                 API ENDPOINTS                        │
├──────────────────────────────────────────────────────┤
│                                                      │
│  GET    /                    → API info             │
│  GET    /health              → Health check         │
│                                                      │
│  ┌──────────── USERS ────────────┐                 │
│  │                                │                 │
│  │ POST   /users                 │ → Create user   │
│  │ GET    /users                 │ → List users    │
│  │ GET    /users/:id             │ → Get user      │
│  │ PUT    /users/:id             │ → Update user   │
│  │ DELETE /users/:id             │ → Delete user   │
│  │                                │                 │
│  └────────────────────────────────┘                 │
│                                                      │
└──────────────────────────────────────────────────────┘
```

## 🎯 Quick Start Timeline

```
┌──────────┐
│ Minute 1 │ Clone repo & run: npm install
└──────────┘

┌──────────┐
│ Minute 2 │ Generate Prisma: npm run prisma:generate
└──────────┘

┌──────────┐
│ Minute 3 │ Start Docker: docker-compose up -d
└──────────┘

┌──────────┐
│ Minute 4 │ Test API: ./scripts/test-api.sh
└──────────┘

┌──────────┐
│ Minute 5 │ 🎉 You're ready!
└──────────┘
```

## 🛠️ Tools & Technologies

```
┌─────────────────────────────────────────────────┐
│                 TECH STACK                      │
├─────────────────────────────────────────────────┤
│                                                 │
│  Language:      TypeScript 5.3                 │
│  Runtime:       Node.js 18+                    │
│  Framework:     Express.js 4.18                │
│  Database:      PostgreSQL 15                  │
│  ORM:           Prisma 5.22                    │
│  Testing:       Keploy (latest)                │
│  Container:     Docker + Compose               │
│                                                 │
└─────────────────────────────────────────────────┘
```

## 📚 Documentation Hierarchy

```
┌─────────────────────────────────────────┐
│     Where to Start?                     │
├─────────────────────────────────────────┤
│                                         │
│  New to project?                        │
│  └─▶ GETTING_STARTED.md                │
│                                         │
│  Need quick commands?                   │
│  └─▶ QUICKSTART.md                     │
│                                         │
│  Want full details?                     │
│  └─▶ README.md                         │
│                                         │
│  Need overview?                         │
│  └─▶ OVERVIEW.md                       │
│                                         │
│  Want to see what's built?              │
│  └─▶ PROJECT_SUMMARY.md                │
│                                         │
└─────────────────────────────────────────┘
```

## ✅ Success Path

```
Step 1: Verify Setup
  └─▶ ./scripts/verify-setup.sh

Step 2: Start Application  
  └─▶ npm run dev or docker-compose up -d

Step 3: Test API
  └─▶ ./scripts/test-api.sh

Step 4: Record with Keploy
  └─▶ npm run keploy:record

Step 5: Run Tests
  └─▶ npm run keploy:test

Step 6: Deploy
  └─▶ docker-compose up -d

✨ Success! ✨
```

## 🎓 Learning Resources

```
Internal Docs:
├── GETTING_STARTED.md  ← Start here
├── README.md           ← Complete guide
├── QUICKSTART.md       ← Quick reference
└── requests.http       ← API examples

External Links:
├── Keploy:     keploy.io/docs
├── Express:    expressjs.com
├── Prisma:     prisma.io/docs
├── TypeScript: typescriptlang.org
└── Docker:     docs.docker.com
```

---

**Made with ❤️ for developers learning Keploy**
